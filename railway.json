{
  "build": {
    "builder": "nixpacks",
    "config": {
      "buildCommand": "pip install -r requirements.txt",
      "startCommand": "python app.py"
    }
  },
  "deploy": {
    "startCommand": "python app.py"
  }
}
